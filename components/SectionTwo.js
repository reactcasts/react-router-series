'use strict';

import React from 'react';

const SectionTwo = React.createClass({
  render () {
    let style = {
      'background': '#A7DBD8',
      'width': '100%',
      'height': '75%'
    };

    return (
      <div style={style}></div>
    );
  }
});

export default SectionTwo;
