'use strict';

import React from 'react';

const NotFound = React.createClass({
  render () {
    let style = {
      'background': '#FA6900',
      'width': '100%',
      'height': '100%'
    };

    return (
      <div style={style}></div>
    );
  }
});

export default NotFound;
