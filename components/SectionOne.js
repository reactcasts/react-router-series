'use strict';

import React from 'react';

const SectionOne = React.createClass({
  render () {
    let style = {
      'background': '#69D2E7',
      'width': '100%',
      'height': '75%'
    }

    return (
      <div style={style}></div>
    );
  }
});

export default SectionOne;
